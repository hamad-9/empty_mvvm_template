<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hamad.empty_mvvm_template">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.hamad.empty_mvvm_template.MvvmApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity android:name="com.hamad.empty_mvvm_template.ui.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.hamad.empty_mvvm_template.ui.login.LoginActivity"
            android:launchMode="singleTop" />

        <activity
            android:name="com.hamad.empty_mvvm_template.ui.main.MainActivity"
            android:launchMode="singleTop" />

        <!--        <activity-->
        <!--            android:name="com.hamad.mvvmtemplate.ui.feed.FeedActivity"-->
        <!--            android:launchMode="singleTop"-->
        <!--            android:parentActivityName=".ui.main.MainActivity">-->

        <!--            &lt;!&ndash; Parent activity meta-data to support 4.0 and lower &ndash;&gt;-->
        <!--            <meta-data-->
        <!--                android:name="android.support.PARENT_ACTIVITY"-->
        <!--                android:value=".ui.main.MainActivity" />-->
        <!--        </activity>-->
    </application>

</manifest>